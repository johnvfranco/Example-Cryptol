tasks:
  - command: xterm -e export PATH=".:$PATH" && cryptol
  - command: xterm
  - command: vscode
    
vscode:
  extensions:
    # Add PDF viewing support to VSCode
    - tomoki1207.pdf
    - jldodds.cryptol
